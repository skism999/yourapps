# ConoHa VPS デプロイガイド - My Dungeon

このガイドに従って、ConoHa VPSにMy Dungeonアプリケーションをデプロイします。

---

## 📋 必要なもの

- [ ] ConoHa VPSアカウント
- [ ] クレジットカード（月512円プラン）
- [ ] SSH接続用のターミナル（Windows: PowerShell/WSL、Mac/Linux: ターミナル）
- [ ] このプロジェクトのGitリポジトリ（推奨）

---

## 🚀 ステップ1: ConoHa VPSアカウント作成とサーバー構築

### 1.1 アカウント作成

1. **ConoHa VPS公式サイトにアクセス**
   - URL: https://www.conoha.jp/vps/
   - 「今すぐお申し込み」をクリック

2. **アカウント登録**
   - メールアドレスとパスワードを入力
   - SMS認証を完了
   - 氏名・住所を入力
   - クレジットカード情報を登録

### 1.2 サーバー構築

1. **コントロールパネルにログイン**
   - https://manage.conoha.jp/

2. **サーバー追加**
   - 左メニュー「サーバー」→「＋サーバー」をクリック

3. **プラン設定**
   ```
   リージョン: 東京
   プラン: VPS
   メモリ: 1GB (月額512円)
   イメージタイプ: Ubuntu 22.04
   rootパスワード: 強力なパスワードを設定（メモしておく）
   ネームタグ: mydungeon-server
   ```

4. **サーバー起動確認**
   - ステータスが「起動中」になるまで待つ（1〜2分）
   - IPアドレスをメモ（例: 123.456.789.012）

---

## 🔧 ステップ2: サーバーへのSSH接続と初期設定

### 2.1 SSH接続

**Windows (PowerShell):**
```powershell
ssh root@123.456.789.012
# パスワードを入力
```

**Mac/Linux:**
```bash
ssh root@123.456.789.012
# パスワードを入力
```

初回接続時に `Are you sure you want to continue connecting?` と聞かれたら `yes` を入力。

### 2.2 システムアップデート

```bash
# パッケージリストを更新
apt update

# 全パッケージをアップグレード
apt upgrade -y
```

### 2.3 必要なソフトウェアのインストール

```bash
# Git、Docker、Docker Composeをインストール
apt install -y git curl wget

# Dockerのインストール
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Docker Composeのインストール
apt install -y docker-compose

# Dockerの起動と自動起動設定
systemctl start docker
systemctl enable docker

# インストール確認
docker --version
docker-compose --version
```

---

## 📦 ステップ3: プロジェクトのデプロイ

### 3.1 プロジェクトの取得

**オプションA: Gitリポジトリからクローン（推奨）**

```bash
# ホームディレクトリに移動
cd /root

# GitHubからクローン（リポジトリURLを自分のものに変更）
git clone https://github.com/your-username/yourapps.git

# プロジェクトディレクトリに移動
cd yourapps/projects/mydungeon
```

**オプションB: ファイルを直接アップロード**

ローカルマシンから：
```bash
# scpでファイルをアップロード
scp -r /path/to/yourapps/projects/mydungeon root@123.456.789.012:/root/
```

サーバー上で：
```bash
cd /root/mydungeon
```

### 3.2 環境変数の設定

```bash
# backend/.envファイルを作成
cat > backend/.env << 'EOF'
TARGET_URL=https://dungeon.humanjp.com/
HEADLESS=true
PYTHONUNBUFFERED=1
EOF

# 確認
cat backend/.env
```

### 3.3 データベースファイルの確認

```bash
# CSVファイルと画像が存在することを確認
ls -la database/csv/
ls -la database/images/

# 出力ディレクトリの作成
mkdir -p output
chmod 755 output
```

---

## 🏃 ステップ4: アプリケーションの起動

### 4.1 初回デプロイ

```bash
# デプロイスクリプトに実行権限を付与
chmod +x deploy.sh

# デプロイ実行
./deploy.sh
```

初回は10〜15分かかります（DockerイメージのビルドとPlaywrightのインストール）。

### 4.2 起動確認

```bash
# コンテナ状態を確認
docker-compose ps

# ログを確認
docker-compose logs -f mydungeon

# Ctrl+C でログ表示を終了
```

正常に起動すると、以下のようなログが表示されます：
```
mydungeon-app | INFO:     Application startup complete.
mydungeon-app | INFO:     Uvicorn running on http://0.0.0.0:8000
```

---

## 🌐 ステップ5: アクセス確認

### 5.1 ブラウザでアクセス

ブラウザで以下のURLを開く：
```
http://123.456.789.012
```
（IPアドレスは自分のサーバーのものに変更）

### 5.2 動作確認

1. トップページ（入力画面）が表示されることを確認
2. 生年月日と時刻を入力
3. 診断実行
4. 結果画面が表示されることを確認

---

## 🔄 ステップ6: コードの更新方法

コードを変更した後、サーバーに反映する手順：

### 6.1 Gitを使う場合

```bash
# サーバーにSSH接続
ssh root@123.456.789.012

# プロジェクトディレクトリに移動
cd /root/yourapps/projects/mydungeon

# 最新コードを取得してデプロイ
./deploy.sh
```

### 6.2 手動で更新する場合

```bash
# ローカルから変更ファイルをアップロード
scp -r /path/to/modified/files root@123.456.789.012:/root/yourapps/projects/mydungeon/

# サーバーにSSH接続
ssh root@123.456.789.012

# デプロイ
cd /root/yourapps/projects/mydungeon
./deploy.sh
```

---

## 🛠 トラブルシューティング

### コンテナが起動しない

```bash
# ログを確認
docker-compose logs

# コンテナを再起動
docker-compose restart
```

### Playwrightエラー

```bash
# コンテナ内に入る
docker-compose exec mydungeon bash

# Playwrightの再インストール
playwright install chromium
playwright install-deps chromium

# 終了
exit

# コンテナを再起動
docker-compose restart mydungeon
```

### ポート80が使えない

```bash
# ポート80を使っているプロセスを確認
netstat -tuln | grep :80

# Apacheなど他のウェブサーバーを停止
systemctl stop apache2
systemctl disable apache2
```

### メモリ不足

```bash
# メモリ使用状況を確認
free -h

# 不要なDockerイメージを削除
docker system prune -a
```

---

## 🔒 セキュリティ対策（推奨）

### ファイアウォール設定

```bash
# UFWのインストールと設定
apt install -y ufw

# SSH、HTTP、HTTPSを許可
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp

# ファイアウォール有効化
ufw enable

# 状態確認
ufw status
```

### 自動アップデート設定

```bash
# unattended-upgradesのインストール
apt install -y unattended-upgrades

# 有効化
dpkg-reconfigure -plow unattended-upgrades
```

---

## 📊 運用コマンド集

### コンテナ管理

```bash
# コンテナの起動
docker-compose up -d

# コンテナの停止
docker-compose down

# コンテナの再起動
docker-compose restart

# ログ表示
docker-compose logs -f

# 特定サービスのログ
docker-compose logs -f mydungeon
```

### サーバー管理

```bash
# ディスク使用量確認
df -h

# メモリ使用量確認
free -h

# プロセス確認
top

# Dockerのディスク使用量確認
docker system df
```

---

## 🌟 次のステップ（オプション）

### ドメイン設定

1. **ドメインを取得**（お名前.com、ムームードメインなど）

2. **DNSレコード設定**
   - Aレコード: `@` → `123.456.789.012`（サーバーのIPアドレス）

3. **Nginx設定を更新**
   ```bash
   # nginx/conf.d/mydungeon.conf を編集
   nano nginx/conf.d/mydungeon.conf

   # server_name を変更
   server_name yourdomain.com;

   # 保存して再起動
   docker-compose restart nginx
   ```

### SSL証明書（HTTPS化）

```bash
# Certbotコンテナでssl証明書を取得
docker run -it --rm \
  -v /root/yourapps/projects/mydungeon/nginx/ssl:/etc/letsencrypt \
  certbot/certbot certonly --standalone \
  -d yourdomain.com \
  --email your-email@example.com \
  --agree-tos

# Nginx設定のHTTPSセクションを有効化
nano nginx/conf.d/mydungeon.conf
# コメントアウトを外す

# Nginxを再起動
docker-compose restart nginx
```

---

## 💰 料金について

### 月額料金（1GBプラン）

- **基本料金**: 512円/月
- **データ転送量**: 無制限（追加料金なし）
- **ストレージ**: 100GB SSD（追加料金なし）

### 複数アプリを追加する場合

同じVPS内で何個でもアプリを追加可能（追加料金なし）。

**docker-compose.yml に追加：**
```yaml
  app2:
    build: ../app2
    ports:
      - "8001:8000"
    # ... 設定 ...
```

---

## 📞 サポート

### 問題が発生した場合

1. **ログを確認**
   ```bash
   docker-compose logs -f
   ```

2. **ConoHaサポート**
   - https://support.conoha.jp/
   - 日本語サポート対応

3. **プロジェクトのissue**
   - GitHubリポジトリにissueを作成

---

## ✅ チェックリスト

デプロイ完了後、以下を確認：

- [ ] サーバーが起動している
- [ ] http://IPアドレス でアクセスできる
- [ ] トップページが表示される
- [ ] 診断機能が動作する
- [ ] 画像が正しく表示される
- [ ] ファイアウォール設定完了
- [ ] 定期的なバックアップ設定（推奨）

---

**作成日**: 2025-11-26
**対象プラン**: ConoHa VPS 1GB（月512円）
**OS**: Ubuntu 22.04
**所要時間**: 約30分〜1時間
